---
description: 项目默认使用UTF-8格式的中文进行对话和创建文件
alwaysApply: true
---

# 中文优先规则

本项目所有对话与新建文件优先使用 UTF-8 格式的中文。

## 对话语言

- 所有与 AI 的对话（包括提问、回答、讨论）默认使用中文
- 除非用户明确使用其他语言提问，否则 AI 默认用中文回复

## 新建文件

- 所有新建的源代码文件、配置文件、文档等均使用 UTF-8 编码
- 文件内的注释、文本内容优先使用中文
- 生成的文件自动包含 UTF-8 编码声明（如 HTML 的 `<meta charset="UTF-8">`、Python 的 `# -*- coding: utf-8 -*-`）
